import React from "react";

const RecipeSummary = ({summary}) => {
    console.log(summary)
    return (
        <div>       
            {summary}
        </div>
    )
}

export default RecipeSummary;